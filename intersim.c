#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void) {
  srand(time(NULL));
  // todo

  // handle SIGTERM graciously, as we are being told to terminate from kernelsim

  return 0;
}
